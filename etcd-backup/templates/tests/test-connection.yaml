apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "etcd-backup.fullname" . }}-test-connection"
  labels:
    {{- include "etcd-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "etcd-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
